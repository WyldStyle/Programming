let array1store = [], n = 10;
for (let i = 0; i<=n; i++)
{
    array1store.push(i)
}
console.log(array1store);

//max 
let arr1 = arr.sort(function(a,b) { return a - b})
    let length2 = arr1.length;
    //return 
    console.log(arr1[length2 - 1]);

    //return Math.max.apply(null, arr);

    