# Get-Service
# Get-Date
    # now ther's no way to remember all Verb-Noun commandsLets so
# Get-Command
    # now it gives all the commands that are loaded
    # but let's say you remeber the noun but not the verb, then 
# Get-Command -Noun Service
# Get-Command -Verb Get

# Get-Help Get-Service -FulL
# Get-Alias

# cd == Set-Location -Path C:\Users\Public\Programming\PowerShell

Get-ExecutionPolicy